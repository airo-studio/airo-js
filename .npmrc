# pnpm hoisting policy: keep node_modules flat where possible but isolate
# package-private deps so cross-package dep leakage doesn't masquerade as
# correct ("phantom dependency" guard). Strict mode catches missing deps
# in any single package's package.json before publish.
shamefully-hoist=false
strict-peer-dependencies=true
auto-install-peers=true
