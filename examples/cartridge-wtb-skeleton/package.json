{
  "name": "@airo-js-examples/cartridge-wtb-skeleton",
  "version": "0.0.0",
  "private": true,
  "description": "Throwaway type-skeleton: validates the @airo-js/cartridge-kit contract against a real WTB-shaped cartridge (Product/Retailer/Offer schema). Compiles only — does not run.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@airo-js/cartridge-kit": "workspace:*",
    "@airo-js/core": "workspace:*"
  }
}
