{
  "name": "airo-js",
  "private": true,
  "version": "0.0.0",
  "description": "Airo — rendering-only framework for config-driven, plugin-loaded UI.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.4.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r --filter './packages/*' run build",
    "test": "pnpm -r --filter './packages/*' run test",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean && rm -rf node_modules",
    "yalc:push": "pnpm build && pnpm -r --filter './packages/*' exec yalc publish --private",
    "yalc:push:auto": "pnpm build && pnpm -r --filter './packages/*' exec yalc push --private"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
